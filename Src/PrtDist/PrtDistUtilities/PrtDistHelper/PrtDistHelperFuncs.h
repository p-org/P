#pragma once
#include<string>
#include<iostream>
#include <windows.h>
#include <thread>
#include<fstream>

using namespace std;

boolean _ROBOCOPY(string source, string dest);
char* _CONCAT(char* string1, char* string2);